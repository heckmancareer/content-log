import { SortOptionFormatterPipe } from './sort-option-formatter.pipe';

describe('SortOptionFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOptionFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
